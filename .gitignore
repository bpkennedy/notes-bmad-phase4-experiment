# Node.js
# dependencies
/node_modules
.pnp-commit.yaml
.pnp.cjs
.yarn/*
!/.yarn/patches
!/.yarn/plugins
!/.yarn/releases
!/.yarn/sdks
!/.yarn/versions

# production
/dist
/build
/coverage

# environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Prisma
# Keep the generated Prisma Client + query engine out of version control
# Default location is node_modules/.prisma or specified in schema.prisma
# If you specified a custom output path, add it here, e.g., /src/generated/prisma
node_modules/.prisma
/prisma/migrations.sql # Optional: Some teams track migrations in VCS, others don't.
# If you use the default `prisma init` setup, the .env file is handled above.

# IDEs and editors
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/
*.sublime-workspace
*.launch
.settings/
.project
.classpath
.c9/

# OS and temporary files
.DS_Store
Thumbs.db
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
# Diagnostic reports (nodejs.org)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
